# AtlasLoom Context

## Character Profiles Language

- **项目（Project）**：角色档案的顶层工作容器，拥有其角色和标签数据。
- **角色（Character）**：归属于一个项目的角色档案。
- **标签（Tag）**：归属于一个项目、用于分类和筛选角色的标记。
- **项目工作区（Project workspace）**：围绕一个项目管理其角色和标签的当前工作上下文。
- **软删除（Soft deletion）**：将项目或角色从正常工作区隐藏，但保留数据以便恢复；项目和角色的删除状态彼此独立。

## Git Workflow Glossary

- **Issue**: GitHub 上描述需求、缺陷、架构变更或其他可追踪工作的记录，是代码变更的需求事实源。
- **dev**: 日常开发集成分支。普通功能、修复、文档和维护分支从这里派生，并通过 PR 合并回这里。
- **master**: 稳定发布分支。只有经过验证的 `dev` 变更或紧急修复可以通过 PR 进入这里。
- **工作分支**: 从 `dev` 或 `master` 派生、用于隔离一次变更的短期分支；PR 合并后远端和本地都应删除。
- **普通变更**: 从 `dev` 派生的功能、缺陷修复、文档或维护工作。
- **hotfix**: 针对 `master` 上已发布内容的紧急修复。从 `master` 派生，发布后必须回流到 `dev`。
- **发布 PR**: 将 `dev` 合并到 `master` 的 PR，代表一次候选发布，必须记录验证结果和版本说明。
- **回滚**: 通过专门的 `revert` 分支和 PR 撤销已合并变更，不直接改写 `master` 历史。
- **正式发布**: 基于已验证的 `master` 提交创建不可移动的 SemVer tag 和 GitHub Release。
